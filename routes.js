
//need to define callback functions
//need to create controllers for each of the "pages" I am loading
//need to insert the appropriate script tags into my html
page('/', callback1);
page('/Projects', callback2);
page('/SociaMedia', callback3);

page();
